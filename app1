帮我修复两个问题：

【问题一】Header 顶部错位
components/Header.tsx 加载用户状态时有闪烁和错位，修复方案：
- 添加 isLoading 状态，初始值 true
- 获取用户信息完成后设置 isLoading = false
- isLoading 为 true 时 Header 右侧显示空白占位，
  不显示"登录/注册"或用户名，避免布局跳动：
  {isLoading ? (
    <div className="w-24 h-8" />
  ) : user ? (
    <用户名+退出按钮>
  ) : (
    <登录注册按钮>
  )}

【问题二】任何用户名都显示"已被占用"
检查 app/api/auth/register/route.ts 里判断用户名是否存在的逻辑：

1. 打印日志看实际查询结果：
   console.log('查询用户名结果', data, error)

2. 问题很可能是查询语句写错了，正确写法：
   const { data: existingUser, error: queryError } = await supabase
     .from('users')
     .select('id')
     .eq('username', username)
     .maybeSingle()  // 用 maybeSingle 而不是 single
   
   console.log('existingUser:', existingUser, 'queryError:', queryError)
   
   // 只有 existingUser 不为 null 才说明用户名被占用
   if (existingUser) {
     return Response.json(
       { error: '用户名已被占用' },
       { status: 400 }
     )
   }

3. 修复后重新部署：
   git add .
   git commit -m "fix: 修复注册用户名误报已占用 + Header 错位"
   git push

完成后告诉我注册接口的日志输出。